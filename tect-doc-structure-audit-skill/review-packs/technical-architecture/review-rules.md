# 技术文档结构审核规则

本规则包只审核 `block-formatting-context.json` 中的文本章节、标题、正文段落及其格式。表格、图片、嵌入对象和上下文以外的内容不在范围内。

在填入完整规则前，不执行任何实质性审核判断。

## 全局审核口径

- 审核目标：[由规则维护者填写]
- 适用文档范围：[由规则维护者填写]
- 默认审核维度：[由规则维护者填写]
- 默认严重级别：[由规则维护者填写]
- Finding 的统一判定口径：[由规则维护者填写]
- 证据与引文要求：[由规则维护者填写]

## 规则模板

复制以下模板，为每条结构审核规则创建一个独立小节。规则填写完整后才可执行。

### [规则编号]

- 规则名称：[由规则维护者填写]
- 审核对象：[章节标题 / 正文段落 / 标题层级 / 段落格式 / 由规则维护者填写]
- 适用范围：[由规则维护者填写]
- 审核目标：[由规则维护者填写]
- 触发条件：[由规则维护者填写]
- 不触发或豁免条件：[由规则维护者填写]
- 判定结论：[由规则维护者填写]
- 严重级别：[由规则维护者填写]
- 审核维度与分类：[由规则维护者填写]
- 必要证据：[由规则维护者填写；仅可引用标题或正文的 `block_index` 与连续原文]
- 整改建议：[由规则维护者填写]
- 完成标准：[由规则维护者填写]
